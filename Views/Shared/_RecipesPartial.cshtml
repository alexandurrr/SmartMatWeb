@model IEnumerable<Recipe>


<h2>Søketreff:</h2>
<ul>
    
    @foreach (var r in Model)
    {
        <li>@r.Title <br>
        @r.Image <img src = "@r.Image" alt="Matbilde"><br>
            @r.ApplicationUser</li>  // kan liste title med bilde og user her etterhvert? Med link til full oppskrift?
    }
    
</ul>